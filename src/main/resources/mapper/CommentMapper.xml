<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fqh.dao.CommentMapper">


<!--    public int insertComment(Comment comment)-->
    <insert id="insertComment">
        insert into tb_content (user_name, content, content_time, starts, likes_number)
            values (#{comment.userName}, #{comment.content}, #{comment.contentTime}, #{comment.stars}, #{comment.likesNumber})
    </insert>
</mapper>